git rm <file1.html> <file2.html> ...   # remove files from both git and working directory
git commit -m "Remove accidentally added HTML files"
git push origin main                    # or your branch name
